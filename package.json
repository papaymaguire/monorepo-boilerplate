{
  "name": "@papaymaguire/monorepo-config",
  "version": "1.0.0",
  "description": "A preconfigured monorepo setup for full stack web app development.",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "test": "jest",
    "deploy": ""
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/eslint-config",
    "packages/typescript-config",
    "packages/test-package"
  ],
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "prettier": "3.0.2",
    "ts-jest": "^29.1.1",
    "turbo": "^1.10.13"
  }
}
