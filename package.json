{
  "name": "@papaymaguire/monorepo-boilerplate",
  "version": "1.0.0",
  "description": "A preconfigured monorepo setup for full stack web app development.",
  "scripts": {
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "test": "jest",
    "deploy": ""
  },
  "workspaces": [
    "packages/eslint-config",
    "packages/typescript-config",
    "packages/test-package"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "prettier": "3.0.2",
    "turbo": "^1.10.13"
  }
}
